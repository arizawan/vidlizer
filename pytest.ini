[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html
markers =
    e2e: end-to-end tests requiring real network and/or API key (deselected by default)
filterwarnings =
    ignore:builtin type.*has no __module__:DeprecationWarning
